{
  "name": "clickup-margin-report-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test scripts/*.test.mjs",
    "typecheck": "tsc -p jsconfig.json --noEmit",
    "lint:design": "node scripts/design-lint.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "typescript": "^6.0.3"
  }
}
